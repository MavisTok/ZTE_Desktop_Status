<Window x:Class="ZTE.ExtendedInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="扩展信息 - 含5G小区和设备详情" Height="900" Width="1500"
        MinWidth="1400" MinHeight="850"
        Background="#F5F5F5">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="12" CornerRadius="4" Margin="0,0,0,12">
            <Grid>
                <TextBlock Text="扩展信息中心 - 5G/LTE 小区 + 设备硬件"
                          FontSize="22" FontWeight="Bold"
                          Foreground="White"
                          HorizontalAlignment="Left" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="🔄 刷新" Padding="8,4" Margin="0,0,12,0"
                            Command="{Binding RefreshCommand}"
                            Background="#1976D2" Foreground="White" BorderThickness="0" />
                    <TextBlock Text="更新: " Foreground="White" VerticalAlignment="Center" />
                    <TextBlock Text="{Binding LastUpdate}" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content - 4 Rows of Cards -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Row 1: WiFi Info (4 cards) -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- WiFi Status -->
                    <Border Grid.Column="0" Background="White" CornerRadius="4" Padding="15" Margin="0,0,5,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="WiFi状态" FontSize="15" FontWeight="Bold" Foreground="#2196F3" Margin="0,0,0,10" />
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="总开关:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding WifiStatus}" FontWeight="Bold" Foreground="#4CAF50" FontSize="12" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="负载均衡:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding LbdStatus}" FontWeight="SemiBold" FontSize="12" /></StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- 2.4G WiFi -->
                    <Border Grid.Column="1" Background="White" CornerRadius="4" Padding="15" Margin="5,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="2.4G WiFi" FontSize="15" FontWeight="Bold" Foreground="#2196F3" Margin="0,0,0,10" />
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="SSID:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding Wifi2gSsid}" FontWeight="Bold" FontSize="12" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="认证:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding Wifi2gAuthMode}" FontWeight="SemiBold" FontSize="12" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="状态:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding Wifi2gStatus}" FontWeight="SemiBold" Foreground="#4CAF50" FontSize="12" /></StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- 5G WiFi -->
                    <Border Grid.Column="2" Background="White" CornerRadius="4" Padding="15" Margin="5,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="5G WiFi" FontSize="15" FontWeight="Bold" Foreground="#2196F3" Margin="0,0,0,10" />
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="SSID:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding Wifi5gSsid}" FontWeight="Bold" FontSize="12" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="认证:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding Wifi5gAuthMode}" FontWeight="SemiBold" FontSize="12" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="状态:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding Wifi5gStatus}" FontWeight="SemiBold" Foreground="#4CAF50" FontSize="12" /></StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Battery -->
                    <Border Grid.Column="3" Background="White" CornerRadius="4" Padding="15" Margin="5,0,0,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="电池状态" FontSize="15" FontWeight="Bold" Foreground="#2196F3" Margin="0,0,0,10" />
                            <TextBlock Text="{Binding BatteryPercent}" FontSize="26" FontWeight="Bold" Foreground="#4CAF50" HorizontalAlignment="Center" Margin="0,5" />
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="温度:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding BatteryTemperature}" FontWeight="SemiBold" Foreground="#FF9800" FontSize="12" /></StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Row 2: 5G NR Info (4 cards) -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- 5G NR Signal -->
                    <Border Grid.Column="0" Background="White" CornerRadius="4" Padding="15" Margin="0,0,5,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="5G NR 信号" FontSize="15" FontWeight="Bold" Foreground="#9C27B0" Margin="0,0,0,10" />
                            <Grid Margin="0,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="65" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="RSRP:" Foreground="#666" FontSize="12" />
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Nr5gRsrp}" FontWeight="Bold" FontSize="12" />
                                    <ProgressBar Height="6" Margin="0,4,0,0" Maximum="100"
                                                 Value="{Binding Nr5gRsrpLevel}"
                                                 Foreground="{Binding Nr5gRsrpBrush}"
                                                 Background="#EEE" />
                                </StackPanel>
                            </Grid>
                            <Grid Margin="0,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="65" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="RSRQ:" Foreground="#666" FontSize="12" />
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Nr5gRsrq}" FontWeight="SemiBold" FontSize="12" />
                                    <ProgressBar Height="6" Margin="0,4,0,0" Maximum="100"
                                                 Value="{Binding Nr5gRsrqLevel}"
                                                 Foreground="{Binding Nr5gRsrqBrush}"
                                                 Background="#EEE" />
                                </StackPanel>
                            </Grid>
                            <Grid Margin="0,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="65" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="SNR:" Foreground="#666" FontSize="12" />
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Nr5gSnr}" FontWeight="SemiBold" FontSize="12" />
                                    <ProgressBar Height="6" Margin="0,4,0,0" Maximum="100"
                                                 Value="{Binding Nr5gSnrLevel}"
                                                 Foreground="{Binding Nr5gSnrBrush}"
                                                 Background="#EEE" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- 5G NR Cell -->
                    <Border Grid.Column="1" Background="White" CornerRadius="4" Padding="15" Margin="5,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="5G NR 小区" FontSize="15" FontWeight="Bold" Foreground="#9C27B0" Margin="0,0,0,10" />
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="PCI:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding Nr5gPci}" FontWeight="Bold" FontSize="12" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="频段:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding Nr5gBand}" FontWeight="SemiBold" FontSize="12" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="带宽:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding Nr5gBandwidth}" FontWeight="SemiBold" FontSize="12" /></StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- LTE Signal -->
                    <Border Grid.Column="2" Background="White" CornerRadius="4" Padding="15" Margin="5,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="LTE 信号" FontSize="15" FontWeight="Bold" Foreground="#FF5722" Margin="0,0,0,10" />
                            <Grid Margin="0,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="65" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="RSRP:" Foreground="#666" FontSize="12" />
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding LteRsrp}" FontWeight="Bold" FontSize="12" />
                                    <ProgressBar Height="6" Margin="0,4,0,0" Maximum="100"
                                                 Value="{Binding LteRsrpLevel}"
                                                 Foreground="{Binding LteRsrpBrush}"
                                                 Background="#EEE" />
                                </StackPanel>
                            </Grid>
                            <Grid Margin="0,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="65" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="RSRQ:" Foreground="#666" FontSize="12" />
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding LteRsrq}" FontWeight="SemiBold" FontSize="12" />
                                    <ProgressBar Height="6" Margin="0,4,0,0" Maximum="100"
                                                 Value="{Binding LteRsrqLevel}"
                                                 Foreground="{Binding LteRsrqBrush}"
                                                 Background="#EEE" />
                                </StackPanel>
                            </Grid>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="PCI:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding LtePci}" FontWeight="SemiBold" FontSize="12" /></StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Network Type -->
                    <Border Grid.Column="3" Background="White" CornerRadius="4" Padding="15" Margin="5,0,0,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="网络类型" FontSize="15" FontWeight="Bold" Foreground="#3F51B5" Margin="0,0,0,10" />
                            <TextBlock Text="{Binding NetworkType}" FontSize="20" FontWeight="Bold" Foreground="#3F51B5" HorizontalAlignment="Center" Margin="0,8" />
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="频段:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding ActiveBand}" FontWeight="SemiBold" FontSize="12" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="CA状态:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding CaStatus}" FontWeight="SemiBold" FontSize="12" /></StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Row 3: Device Info (4 cards) -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Device Info -->
                    <Border Grid.Column="0" Background="White" CornerRadius="4" Padding="15" Margin="0,0,5,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="设备信息" FontSize="15" FontWeight="Bold" Foreground="#607D8B" Margin="0,0,0,10" />
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="设备:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding DeviceName}" FontWeight="Bold" FontSize="11" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="硬件:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding HardwareVersion}" FontWeight="SemiBold" FontSize="11" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="软件:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding SoftwareVersion}" FontWeight="SemiBold" FontSize="11" /></StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- CPU & Memory -->
                    <Border Grid.Column="1" Background="White" CornerRadius="4" Padding="15" Margin="5,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="CPU 和内存" FontSize="15" FontWeight="Bold" Foreground="#607D8B" Margin="0,0,0,10" />
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="CPU:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding CpuUsage}" FontWeight="Bold" Foreground="#FF9800" FontSize="12" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="内存:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding MemoryUsage}" FontWeight="SemiBold" FontSize="12" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="存储:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding StorageUsage}" FontWeight="SemiBold" FontSize="12" /></StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- IMEI & MAC -->
                    <Border Grid.Column="2" Background="White" CornerRadius="4" Padding="15" Margin="5,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="设备标识" FontSize="15" FontWeight="Bold" Foreground="#607D8B" Margin="0,0,0,10" />
                            <StackPanel Margin="0,3"><TextBlock Text="IMEI" Foreground="#666" FontSize="10" /><TextBlock Text="{Binding Imei}" FontWeight="SemiBold" FontFamily="Consolas" FontSize="10" /></StackPanel>
                            <StackPanel Margin="0,3"><TextBlock Text="MAC Address" Foreground="#666" FontSize="10" /><TextBlock Text="{Binding MacAddress}" FontWeight="SemiBold" FontFamily="Consolas" FontSize="10" /></StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Temperature & Uptime -->
                    <Border Grid.Column="3" Background="White" CornerRadius="4" Padding="15" Margin="5,0,0,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="运行状态" FontSize="15" FontWeight="Bold" Foreground="#607D8B" Margin="0,0,0,10" />
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="温度:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding DeviceTemp}" FontWeight="Bold" Foreground="#F44336" FontSize="12" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="运行:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding Uptime}" FontWeight="SemiBold" FontSize="12" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="电源:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding PowerMode}" FontWeight="SemiBold" FontSize="11" /></StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Row 4: Additional Info (4 cards) -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Charging Info -->
                    <Border Grid.Column="0" Background="White" CornerRadius="4" Padding="15" Margin="0,0,5,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="充电信息" FontSize="15" FontWeight="Bold" Foreground="#4CAF50" Margin="0,0,0,10" />
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="充电器:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding ChargerStatus}" FontWeight="Bold" FontSize="12" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="充满:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding TimeToFull}" FontWeight="SemiBold" FontSize="12" /></StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="耗尽:" Width="65" Foreground="#666" FontSize="12" /><TextBlock Text="{Binding TimeToEmpty}" FontWeight="SemiBold" FontSize="12" /></StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Power Mode -->
                    <Border Grid.Column="1" Background="White" CornerRadius="4" Padding="15" Margin="5,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="电源模式" FontSize="15" FontWeight="Bold" Foreground="#9C27B0" Margin="0,0,0,10" />
                            <TextBlock Text="{Binding PowerMode}" FontSize="18" FontWeight="Bold" Foreground="#9C27B0" HorizontalAlignment="Center" Margin="0,10" TextWrapping="Wrap" />
                            <StackPanel Orientation="Horizontal" Margin="0,3"><TextBlock Text="模块:" Width="65" Foreground="#666" FontSize="11" /><TextBlock Text="{Binding PowerModeModule}" FontWeight="SemiBold" FontSize="10" /></StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- 5G Cell ID -->
                    <Border Grid.Column="2" Background="White" CornerRadius="4" Padding="15" Margin="5,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="5G 小区ID" FontSize="15" FontWeight="Bold" Foreground="#9C27B0" Margin="0,0,0,10" />
                            <StackPanel Margin="0,5"><TextBlock Text="Cell ID" Foreground="#666" FontSize="10" /><TextBlock Text="{Binding Nr5gCellId}" FontWeight="SemiBold" FontFamily="Consolas" FontSize="10" TextWrapping="Wrap" /></StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- LTE Band -->
                    <Border Grid.Column="3" Background="White" CornerRadius="4" Padding="15" Margin="5,0,0,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="LTE 频段" FontSize="15" FontWeight="Bold" Foreground="#FF5722" Margin="0,0,0,10" />
                            <StackPanel Margin="0,3">
                                <TextBlock Text="支持频段:" Foreground="#666" FontSize="10" />
                                <TextBlock Text="{Binding LteBand}" FontWeight="SemiBold" Foreground="#FF5722" FontSize="9" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Margin="0,8,0,0"><TextBlock Text="电池模式" Foreground="#666" FontSize="10" /><TextBlock Text="{Binding BatteryMode}" FontWeight="SemiBold" FontSize="11" /></StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Background="White" Padding="10" CornerRadius="4" Margin="0,12,0,0" BorderBrush="#E0E0E0" BorderThickness="1">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" Fill="LightGreen" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <TextBlock Text="状态: " FontSize="11" Foreground="#666" VerticalAlignment="Center" />
                    <TextBlock Text="{Binding StatusMessage}" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center" />
                </StackPanel>
                <TextBlock Text="ZTE 5G/LTE 扩展信息监控 - 实时更新"
                          FontSize="10" Foreground="#999"
                          HorizontalAlignment="Right" VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</Window>