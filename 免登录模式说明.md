# ZTE Router Dashboard - 免登录模式说明

## 🎉 重要更新：支持免登录访问！

已实现无需登录即可获取路由器基本信息的功能！

## ✅ 免登录模式优势

### 之前的问题
- ❌ 需要手动从浏览器获取 Session Token
- ❌ Session Token 会过期，需要定期更新
- ❌ 操作繁琐，使用门槛高

### 现在的解决方案
- ✅ 使用固定的 Session Token：`00000000000000000000000000000000` (32个0)
- ✅ 无需登录即可读取基础信息
- ✅ Session 永不过期
- ✅ 开箱即用，零配置

## 🔧 技术实现

### Session Token 配置

已将默认 Session 设置为免登录模式：

```csharp
// AppSettings.cs (第 18 行)
public static string SessionToken { get; set; } = "00000000000000000000000000000000";
```

### 使用的 API 接口

免登录模式使用以下 5 个 ubus API：

| ID | 对象 | 函数 | 参数 | 说明 |
|----|------|------|------|------|
| 1 | zwrt_router.api | router_get_status_no_auth | {} | 路由器基本状态 (无需认证) |
| 2 | zwrt_zte_mdm.api | get_sim_info_before | {} | SIM 卡信息 (登录前可访问) |
| 3 | zte_nwinfo_api | nwinfo_get_netinfo | {} | 网络类型和信号强度 |
| 4 | zwrt_data | get_wwandst | {source_module:"web", cid:1, type:4} | 速率和流量统计 |
| 5 | zwrt_data | get_wwaniface | {source_module:"web", cid:1, connect_status:""} | WAN 接口状态 |

### 完整请求示例

```json
[
  {"jsonrpc":"2.0","id":1,"method":"call","params":["00000000000000000000000000000000","zwrt_router.api","router_get_status_no_auth",{}]},
  {"jsonrpc":"2.0","id":2,"method":"call","params":["00000000000000000000000000000000","zwrt_zte_mdm.api","get_sim_info_before",{}]},
  {"jsonrpc":"2.0","id":3,"method":"call","params":["00000000000000000000000000000000","zte_nwinfo_api","nwinfo_get_netinfo",{}]},
  {"jsonrpc":"2.0","id":4,"method":"call","params":["00000000000000000000000000000000","zwrt_data","get_wwandst",{"source_module":"web","cid":1,"type":4}]},
  {"jsonrpc":"2.0","id":5,"method":"call","params":["00000000000000000000000000000000","zwrt_data","get_wwaniface",{"source_module":"web","cid":1,"connect_status":""}]}
]
```

## 📊 可获取的数据

### 1. 路由器状态 (id=1)
- WAN 连接状态
- WAN 模式
- 自动模式设置

### 2. SIM 卡信息 (id=2)
- ✅ SIM 卡状态 (sim ready)
- ✅ 运营商名称 (CMCC)
- ✅ MCC/MNC 代码
- ✅ MAC 地址
- ✅ 调制解调器状态
- ✅ 双卡支持信息

### 3. 网络和信号信息 (id=3)
- ✅ 网络类型 (5G SA/NSA, 4G LTE)
- ✅ 信号格数 (0-5)
- ✅ 激活频段 (如 n28)
- ✅ 运营商名称
- ✅ 5G 信号参数：
  - RSRP (参考信号接收功率)
  - RSRQ (参考信号接收质量)
  - SNR (信噪比)
  - RSSI (接收信号强度)
- ✅ 4G 信号参数 (LTE RSRP/RSRQ/SNR/RSSI)
- ✅ 小区信息 (Cell ID, PCI, 频道等)

### 4. 流量统计 (id=4)
- ✅ 实时上传/下载速度
- ✅ 实时累计流量
- ✅ 今日流量
- ✅ 本月流量
- ✅ 总流量
- ✅ 峰值速度记录

### 5. WAN 接口状态 (id=5)
- ✅ 连接状态
- ✅ IPv4 地址、网关、DNS
- ✅ IPv6 地址、网关、DNS
- ✅ 设备接口名称

## 🚀 使用方法

### 方法 1: 默认配置 (推荐)

**无需任何配置！** 直接编译运行即可：

1. 打开项目：`ZTE.csproj`
2. 编译：按 `Ctrl + Shift + B`
3. 运行：按 `F5`

### 方法 2: 自定义路由器地址

如果路由器地址不是 `192.168.0.1`：

编辑 `AppSettings.cs` (第 11 行):
```csharp
public static string RouterUrl { get; set; } = "http://192.168.1.1";  // 改为你的路由器地址
```

### 方法 3: 使用已登录的 Session (高级)

如果需要访问更多需要登录的 API，可以使用真实 Session：

1. 浏览器访问路由器管理页面
2. F12 → Network → 找到 `/ubus/` 请求
3. 复制 session 值
4. 更新 `AppSettings.cs` (第 18 行):
   ```csharp
   public static string SessionToken { get; set; } = "你的真实session";
   ```

## ⚠️ 限制说明

### 免登录模式可以访问：
- ✅ 网络状态和连接信息
- ✅ SIM 卡基本信息
- ✅ 信号强度和网络类型
- ✅ 流量统计
- ✅ WAN 接口状态

### 免登录模式无法访问：
- ❌ 连接设备列表详情
- ❌ WiFi 配置修改
- ❌ 路由器设置修改
- ❌ 路由器重启等管理操作

如需访问这些功能，需要使用真实的 Session Token (方法 3)。

## 📝 示例返回数据

### SIM 信息示例
```json
{
  "sim_states": "sim ready",
  "Operator": "CMCC",
  "mdm_mcc": "460",
  "mdm_mnc": "08",
  "modem_main_state": "modem_init_complete",
  "wlan_mac_address": "5c:4d:bf:a2:43:e5"
}
```

### 网络信息示例
```json
{
  "network_type": "SA",
  "signalbar": "5",
  "wan_active_band": "n28",
  "network_provider": "CMCC",
  "nr5g_rsrp": -88,
  "nr5g_rsrq": -11,
  "nr5g_snr": "12.8"
}
```

### 流量统计示例
```json
{
  "real_tx_speed": 2165,
  "real_rx_speed": 168,
  "day_tx_bytes": 2255310482,
  "day_rx_bytes": 2167130632,
  "month_tx_bytes": 12951029674,
  "month_rx_bytes": 27065065610
}
```

## 🎯 总结

免登录模式让 ZTE Router Dashboard 真正实现了：

1. **零配置启动** - 修改路由器地址即可，无需手动获取 Session
2. **稳定可靠** - Session 永不过期
3. **开箱即用** - 适合大多数监控需求
4. **安全友好** - 只读取信息，不修改设置

这是一个重大改进，极大降低了使用门槛！🎉

---

**文档版本**: 1.0
**更新日期**: 2026-01-08
**适用版本**: ZTE Router Dashboard 1.0+
